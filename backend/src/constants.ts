export enum Role {
  Investor = 'investor',
  Company = 'company',
}

export const uploadsPath = 'public/uploads';

export const queryOperators = {
  eq: '=',
  not: '!=',
  gte: '>=',
  lte: '<=',
  like: 'LIKE',
};
